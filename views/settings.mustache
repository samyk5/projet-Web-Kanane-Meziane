<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Paramètres du compte</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .profile-preview {
      width: 100px;
      height: 100px;
      object-fit: cover;
      border-radius: 50%;
      border: 2px solid #ccc;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>

<div class="container mt-5">
  <h2 class="mb-4">⚙️ Paramètres du compte</h2>

  <form method="POST" action="/settings/update" enctype="multipart/form-data">
    <!-- Nom -->
    <div class="mb-3">
      <label for="name" class="form-label">Nom complet</label>
      <input type="text" class="form-control" id="name" name="name" value="{{user.name}}" required>
    </div>

    <!-- Email -->
    <div class="mb-3">
      <label for="email" class="form-label">Adresse email</label>
      <input type="email" class="form-control" id="email" name="email" value="{{user.email}}" required>
    </div>

    <!-- Téléphone -->
    <div class="mb-3">
      <label for="phone" class="form-label">Téléphone</label>
      <input type="tel" class="form-control" id="phone" name="phone" value="{{user.phone}}">
    </div>

    <!-- Localisation -->
    <div class="mb-3">
      <label for="location" class="form-label">Localisation</label>
      <input type="text" class="form-control" id="location" name="location" value="{{user.location}}">
    </div>

    <!-- Présentation -->
    <div class="mb-3">
      <label for="bio" class="form-label">Présentation</label>
      <textarea class="form-control" id="bio" name="bio" rows="4">{{user.bio}}</textarea>
    </div>

    <!-- Photo de profil -->
    <div class="mb-3">
      <label class="form-label">Photo de profil</label><br>

      {{#user.profile_picture}}
        <img src="{{user.profile_picture}}" alt="Photo actuelle" class="profile-preview"><br>
      {{/user.profile_picture}}

      <input type="file" class="form-control mb-2" name="profile_picture_file" accept="image/*">
      <small class="text-muted">ou entrez une URL ci-dessous :</small>
      <input type="url" class="form-control mt-1" name="profile_picture" value="{{user.profile_picture}}">
    </div>

    <!-- Bouton -->
    <button type="submit" class="btn btn-success">💾 Enregistrer</button>
  </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
