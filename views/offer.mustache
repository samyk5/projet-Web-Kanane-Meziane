{{> header}}

<div class="offer-container">
  <div class="offer-card">
    <h1>{{offer.title}}</h1>
    <p class="description">{{offer.description}}</p>

    <div class="details-grid">
      <div><strong>Quantité :</strong> {{offer.quantity}}</div>
      <div><strong>Expiration :</strong> {{offer.expiration_date}}</div>
      <div><strong>Lieu :</strong> 📍 {{offer.location}}</div>
      <div><strong>Date de publication :</strong> {{offer.created_at}}</div>
    </div>

    <div class="status-box {{offer.status}}">
      Statut : <span>{{offer.status}}</span>
    </div>

    <div class="owner-info">
      <h3>👤 Donateur :</h3>
      <a href="/profile/{{offer.owner_id}}" class="owner-link">
        Voir le profil de {{offer.owner_name}}
      </a>
    </div>
  </div>
</div>

{{> footer}}
