<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .offer-header {
      background-color: #f8f9fa;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 20px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .offer-header h1 {
      font-size: 2.5rem;
      font-weight: bold;
    }

    .offer-image {
      max-height: 300px;
      object-fit: cover;
      border-radius: 8px;
    }

    .offer-details {
      margin-top: 20px;
    }

    .offer-card {
      background-color: #ffffff;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      padding: 20px;
      margin-top: 20px;
    }

    .offer-footer {
      text-align: center;
      padding: 10px;
      margin-top: 30px;
    }

    .btn-custom {
      background-color: #28a745;
      color: white;
      border-radius: 20px;
      padding: 12px 30px;
      font-weight: bold;
      border: none;
    }

    .btn-custom:hover {
      background-color: #218838;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <!-- Header Section -->
    <div class="offer-header text-center">
      <h1>{{title}}</h1>
      <p class="lead">{{description}}</p>
    </div>

    <!-- Offer Image -->
    <div class="row">
      <div class="col-md-8 mx-auto">
        <img src="https://via.placeholder.com/800x300" alt="{{title}}" class="offer-image img-fluid">
      </div>
    </div>

    <!-- Offer Details -->
    <div class="row offer-details">
      <div class="col-md-8 mx-auto">
        <div class="offer-card">
          <h4 class="text-primary">Détails de l'Offre</h4>
          <ul class="list-unstyled">
            <li><strong>Quantité :</strong> {{quantity}}</li>
            <li><strong>Date limite de consommation :</strong> {{expiration_date}}</li>
            <li><strong>Localisation :</strong> {{location}}</li>
          </ul>
        </div>

        <!-- Contact/Action -->
        <div class="offer-footer">
           {{#session.isBeneficiary}}
              <form action="/requests/create" method="POST">
                <input type="hidden" name="offer_id" value="{{id}}">
                <button class="btn btn-custom" type="submit">Demander</button>
              </form>
            {{/session.isBeneficiary}} 
        </div>
      </div>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.1/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
