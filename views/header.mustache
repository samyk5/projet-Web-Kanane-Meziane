<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Plateforme Anti-Gaspillage</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="bg-primary text-white p-3">
    <div class="container">
      <h1 class="text-center">Plateforme Anti-Gaspillage Alimentaire</h1>
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
              <li class="nav-item"><a class="nav-link" href="/">Accueil</a></li>

              <!-- Si l'utilisateur est connecté, afficher son nom et la déconnexion -->
              {{#session}}
                <li class="nav-item">
                  <span class="nav-link">Bonjour, {{session.name}}</span> <!-- Affiche le nom de l'utilisateur -->
                </li>
                
                <!-- Si l'utilisateur est un donateur, afficher un lien pour publier une annonce -->
                {{#session.isDonator}}
                  <li class="nav-item">
                    <a class="nav-link" href="/offers/new">Publier une annonce</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" href="/offers/my-offers">Mes annonces</a>
                  </li>
                {{/session.isDonator}}

                <!-- Si l'utilisateur est un bénéficiaire, afficher un lien pour consulter les offres -->
                {{#session.isBeneficiary}}
                  <li class="nav-item">
                    <a class="nav-link" href="/offers">Consulter les offres disponibles</a>
                  </li>
                {{/session.isBeneficiary}}

                <li class="nav-item">
                  <a class="nav-link" href="/auth/logout">
                    <i class="bi bi-box-arrow-right"></i> Déconnexion
                  </a>
                </li>
              {{/session}}

              <!-- Si l'utilisateur n'est pas connecté, afficher les liens de Connexion et Inscription -->
              {{^session}}
                <li class="nav-item"><a class="nav-link" href="/auth/login">Connexion</a></li>
                <li class="nav-item"><a class="nav-link" href="/auth/signup">Inscription</a></li>
              {{/session}}
            </ul>
          </div>
        </div>
      </nav>
    </div>
  </header>
  <main class="container mt-4">
