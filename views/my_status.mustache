{{> header}}

<div class="container mt-5">
  <h2 class="mb-4 text-center">📦 Mes demandes de nourriture</h2>

  {{#requests.length}}
  <div class="requests-list">
    {{#requests}}
      <div class="request-card {{cssStatus}}">
        <h3>{{title}}</h3>
        <p><strong>Description :</strong> {{description}}</p>
        <p><strong>Lieu de retrait :</strong> {{location}}</p>
        <p><strong>Demandée le :</strong> {{request_date}}</p>
        <p class="status">
          🛈 Statut :
          <a href="/offers/{{offer_id}}" class="status-link {{cssStatus}}">
            <span>{{displayStatus}}</span>
          </a>
        </p>
      </div>
    {{/requests}}
  </div>
  {{/requests.length}}

  {{^requests.length}}
  <div class="alert alert-info text-center">
    Vous n'avez encore fait aucune demande.
  </div>
  {{/requests.length}}
</div>

{{> footer}}
